# Opt-in Railway preview environments, gated on the "preview" label.
#
# Label a PR "preview"  -> creates Railway env pr-<number> from production,
#                          pointed at the PR branch, comments the api URL.
# Push more commits     -> Railway auto-deploys (the env's triggers track the
#                          branch), no Action run needed.
# Remove the label      -> environment deleted.
# Close or merge the PR -> environment deleted (if it was labeled).
#
# Runs the Railway CLI via Railway's official container image
# (ghcr.io/railwayapp/cli, pinned below). A Railway API change becomes a bump of
# the image tag, no code change here. The CLI logic lives in
# infra/railway-preview.sh (POSIX sh, so it runs in the minimal image).
#
# Auth is the RAILWAY_WORKSPACE_TOKEN repo secret (a workspace token), passed to
# the CLI as RAILWAY_API_TOKEN; the project-scoped token can't manage environments
# (see infra/README.md). Fork PRs don't receive secrets, so this only works for
# branches in this repo, which is the intended scope. The PR-comment job runs on a
# normal runner because the CLI image has no `gh`.

name: preview-env

on:
  pull_request:
    types: [labeled, unlabeled, closed, reopened]

concurrency:
  group: preview-env-${{ github.event.pull_request.number }}
  cancel-in-progress: false

jobs:
  up:
    if: >-
      (github.event.action == 'labeled' && github.event.label.name == 'preview') ||
      (github.event.action == 'reopened' && contains(github.event.pull_request.labels.*.name, 'preview'))
    runs-on: ubuntu-latest
    # bump this tag to pick up Railway CLI / API changes
    container: ghcr.io/railwayapp/cli:4.50.0
    outputs:
      preview_url: ${{ steps.up.outputs.PREVIEW_URL }}
    steps:
      - uses: actions/checkout@v4

      - name: Create or refresh the preview environment
        id: up
        env:
          RAILWAY_API_TOKEN: ${{ secrets.RAILWAY_WORKSPACE_TOKEN }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
          PR_BRANCH: ${{ github.event.pull_request.head.ref }}
        run: |
          sh infra/railway-preview.sh up "$PR_NUMBER" "$PR_BRANCH" | tee up.log
          grep '^PREVIEW_URL=' up.log >> "$GITHUB_OUTPUT"

  comment:
    needs: up
    runs-on: ubuntu-latest
    permissions:
      pull-requests: write
    steps:
      - name: Comment the preview URL on the PR
        env:
          GH_TOKEN: ${{ github.token }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
          PREVIEW_URL: ${{ needs.up.outputs.preview_url }}
        run: |
          {
            echo "Railway preview environment is up: $PREVIEW_URL"
            echo
            echo "Deploys track this branch automatically. Migrations do NOT run here (shared prod database); if this PR adds a migration, the preview api may not match the schema. Remove the \`preview\` label or close the PR to tear it down."
          } > comment.md
          gh pr comment "$PR_NUMBER" --repo "$GITHUB_REPOSITORY" --body-file comment.md

  down:
    if: >-
      (github.event.action == 'unlabeled' && github.event.label.name == 'preview') ||
      (github.event.action == 'closed' && contains(github.event.pull_request.labels.*.name, 'preview'))
    runs-on: ubuntu-latest
    container: ghcr.io/railwayapp/cli:4.50.0
    steps:
      - uses: actions/checkout@v4

      - name: Delete the preview environment
        env:
          RAILWAY_API_TOKEN: ${{ secrets.RAILWAY_WORKSPACE_TOKEN }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
        run: sh infra/railway-preview.sh down "$PR_NUMBER"
