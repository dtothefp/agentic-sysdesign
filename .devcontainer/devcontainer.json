{
  "name": "sysdesign",
  "dockerComposeFile": "compose.yml",
  "service": "workspace",
  "workspaceFolder": "/workspace",
  "postCreateCommand": "cd backend && uv sync",
  "postStartCommand": "echo 'Dev container ready. Postgres is a sibling at db:5432 (no host port). First time: cd backend, then run make db-init, then make drills.'",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "ms-python.vscode-pylance"
      ],
      "settings": {
        "terminal.integrated.defaultProfile.linux": "bash"
      }
    }
  }
}
