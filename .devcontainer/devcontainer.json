{
  "name": "sysdesign",
  "dockerComposeFile": "compose.yml",
  "service": "workspace",
  "workspaceFolder": "/workspace",
  "postCreateCommand": "pnpm install && uv sync --all-packages",
  "postStartCommand": "echo 'Dev container ready. Postgres is a sibling at db:5432 (no host port). First time: moon run core:db-init, then moon run core:drills (from the repo root).'",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "ms-python.vscode-pylance"
      ],
      "settings": {
        "terminal.integrated.defaultProfile.linux": "bash"
      }
    }
  }
}
