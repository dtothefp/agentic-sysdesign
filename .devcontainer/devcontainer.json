{
  "name": "sysdesign",
  "dockerComposeFile": "compose.yml",
  "service": "workspace",
  "workspaceFolder": "/workspace",
  "postCreateCommand": "uv sync --all-packages",
  "postStartCommand": "echo 'Dev container ready. Postgres is a sibling at db:5432 (no host port). First time: run make db-init, then make drills (from the repo root).'",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "ms-python.vscode-pylance"
      ],
      "settings": {
        "terminal.integrated.defaultProfile.linux": "bash"
      }
    }
  }
}
