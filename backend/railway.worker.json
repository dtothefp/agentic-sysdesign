{
  "$schema": "https://railway.com/railway.schema.json",
  "deploy": {
    "startCommand": "uv run celery -A worker.celery_app worker --beat --loglevel INFO --concurrency 4",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
